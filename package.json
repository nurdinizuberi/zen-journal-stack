{
  "name": "zen-journal-stack",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack journaling app: Express + Prisma backend and Next.js frontend, run with a single command.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend -- -p 3002",
    "build": "npm run build --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm run start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
